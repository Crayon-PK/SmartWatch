#pragma once

#include "controller/AppController.h" 

// -- 类定义 ----------------------------------------------------------------------
class MenuFactory {
public:
    static void assemble(AppController* app);  
};